# TimeSeriesForecasting
A production-grade benchmark framework designed to stress-test deep time-series models (DLinear, Linear_v2) against zero-parameter baselines. It simulates live telemetry loops by streaming sequences window-by-window, injecting dynamic operational faults (drift, missingness) to evaluate real-world resilience over pristine test horizons.
